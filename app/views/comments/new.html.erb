<%= form_with model: @comments, url: user_post_comments_path  do |form| %>
	<h1>Create a new Comment</h1>

	<%= form.label :text %>
	<%= form.text_field :text %>

	<%= form.submit "Submit" %>

<%end%>
