<%= form_with model: @posts, url: user_posts_path  do |form| %>
	<h1>Create a New Post</h1>
	<%= form.label :title %>
	<%= form.text_field :title %>

	<%= form.label :text %>
	<%= form.text_field :text %>

	<%= form.submit "Submit" %>

<%end%>
